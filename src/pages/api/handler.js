// Example Supabase API handler
// Cursor should generate full CRUD endpoints based on the spec

export default async function handler(req, res) {
  // Placeholder logic
  res.status(200).json({ message: "API is working" });
}
